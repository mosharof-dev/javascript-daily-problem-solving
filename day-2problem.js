let password = "mosharof123"

    if(password.length > 8){
        console.log("Strong password");
    }else{
        console.log('Weak password');
    }
const numbers = [10, 25, 8, 47, 3, 99, 15];

for(let i = 0; i < numbers.length; i++){
    if(numbers[i] > 20){
        console.log(numbers[i]);
    }
}